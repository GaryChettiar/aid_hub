{
  "buildCommand": "flutter/bin/flutter build web --release",
  "installCommand": "if [ ! -d \"flutter\" ]; then git clone -b stable --depth 1 https://github.com/flutter/flutter.git; fi && flutter/bin/flutter doctor && flutter/bin/flutter config --enable-web && flutter/bin/flutter pub get",
  "outputDirectory": "build/web"
}